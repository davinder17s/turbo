<?php
/**
 * Twig Filters extensions
 *
 * Example:
 *
 * return array(
 *    'date' => function($timestamp){
 *         return date('D d M y', $timestamp);
 *     }
 * );
 *
 * Usage in your view files:
 *
 * {{ post.timestamp | date }}
 *
 */

return array(

);